import React from "react";
import { Element } from "react-scroll";

export default function Skills() {
  return (
    <React.Fragment>
      <Element
        id="skills-section"
        name="skills-section"
        style={{ height: "100vh" }}
      >
        This is Omid's skills section!
      </Element>
    </React.Fragment>
  );
}
